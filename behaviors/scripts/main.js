// Script principal del mod
// Aquí iría la lógica principal del mod

const MOD_NAME = "Mi Primer Mod";
const MOD_VERSION = "1.0.0";

console.log(`${MOD_NAME} v${MOD_VERSION} cargado correctamente`);

// Exportar funciones principales
module.exports = {
  name: MOD_NAME,
  version: MOD_VERSION
};
